my_fruits_in_fridge = ["apple", "mango", "orange"]

println("array length: ", length(my_fruits_in_fridge))

for (array_item_index, array_item) in enumerate(my_fruits_in_fridge)
    println(array_item_index)
end

# array length: 3
# 1
# 2
# 3